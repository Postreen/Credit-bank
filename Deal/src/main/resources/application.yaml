spring:
  main:
    banner-mode: off
  application:
    name: Deal
  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: none
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/deal_db
    driver-class-name: org.postgresql.Driver
  cloud:
    openfeign:
      client:
        config:
          default:
            logger-level: full

server:
  port: 8081

springdoc:
  api-docs:
    path: /deal/api-docs
  swagger-ui:
    path: /deal/api-docs-ui

client:
  calculator:
    url: http://localhost:8080

logging:
  level:
    root: info
